<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>FoodTech - Главная</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
    <div class="container mt-5">
        <div class="row">
            <div class="col-md-12 text-center">
                <h1>Добро пожаловать в FoodTech</h1>
                <p class="lead">Система управления рестораном</p>
                
                <div class="mt-4">
                    <a href="/users" class="btn btn-primary me-2">Управление пользователями</a>
                    <a href="/health" class="btn btn-outline-secondary">Проверка здоровья системы</a>
                </div>

                <% if (typeof healthStatus !== 'undefined') { %>
                    <div class="mt-4 alert <%= healthStatus === 'OK' ? 'alert-success' : 'alert-danger' %>">
                        Статус API: <%= healthStatus %>
                    </div>
                <% } %>
            </div>
        </div>
    </div>
</body>
</html>
